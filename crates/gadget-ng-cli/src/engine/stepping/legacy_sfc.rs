//! Legacy distributed SFC path (Morton Z-order 3D, halos de partículas).
#![allow(dead_code)]
//!
//! TODO: Extraer `} else if use_sfc { ... }` (~144 líneas).
//! Rama más simple — candidata a ser la primera en extraerse.

/// Placeholder — el código real reside en `super::run_stepping()` mod.rs.
pub(crate) const LEGACY_SFC_EXTRACTED: bool = false;
