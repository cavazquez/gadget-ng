//! Legacy distributed slab tree path (halos punto-a-punto en x).
#![allow(dead_code)]
//!
//! TODO: Extraer `} else if use_dtree { ... }` (~254 líneas).
//! Segunda rama más simple — candidata a extracción temprana.

/// Placeholder — el código real reside en `super::run_stepping()` mod.rs.
pub(crate) const LEGACY_SLAB_EXTRACTED: bool = false;
