# Roadmap

> **Estado al 23 de abril de 2026:** Phases 1–65 + Rápidas + Grandes G1/G2/G3/G4/G5 completadas.  
> Todas las fases Grandes originales están implementadas.  
> Trabajos de largo plazo: ver [`future-grandes.md`](reports/2026-04-future-grandes.md).

---

## Phases completadas (1–65 + Rápidas + G1/G2/G3/G4/G5)

| Rango | Descripción |
|-------|-------------|
| 1–9 | N-body directo, leapfrog, BH, FMM, PM, TreePM, SPH, GPU wgpu, MPI árbol |
| 10–16 | HPC: SFC, LET, Hilbert 3D, SoA+SIMD, FMM octupolo, sistema unidades |
| 17–25 | Cosmología ΛCDM, PM periódico, PM distribuido allreduce/slab/pencil, TreePM scatter-gather |
| 26–36 | ICs ZA/1LPT/2LPT/EH/σ₈, P(k), pk_correction A_grid+R(N) |
| 37–47 | Convención NormalizationMode, validación vs CLASS, Halofit, unidades QKSL |
| 48–55 | HMF PS/ST, perfiles NFW, c(M) Duffy/Bhattacharya, D²(a), FoF vs HMF hasta z=0 |
| 56 | ⏱️ Block timesteps jerárquicos acoplados al árbol LET distribuido |
| 57 | 🟢🔴 PM solver CUDA/HIP (segunda cadena de compilación) |
| 58 | 📐 c(M) + ξ(r) desde N-body (Ludlow+2016, FFT+pares) |
| 59 | 💾 Checkpoint/restart bit-a-bit robusto |
| 60 | 🔀 Domain decomposition adaptativa por costo |
| Rápidas | 📊 CLI `analyze` + 🖼️ PPM render sin dependencias |
| 61 | ⚡ FoF paralelo MPI (cross-boundary Union-Find) |
| 62 | 🌳 Merger trees single-pass (historia de ensamble) |
| 63 | 📈 Análisis in-situ en loop stepping |
| 64 | 🖼️ Proyecciones adicionales + mapa de densidad Viridis + PNG |
| 65 | 💾 HDF5 paralelo MPI-IO (colectivo, layout GADGET-4) |
| **Phase 66** | 💧 **SPH cosmológico integrado**: `ParticleType`, `internal_energy`, `smoothing_length` en `Particle`; `sph_cosmo_kdk_step`, enfriamiento atómico H+He, hook `maybe_sph!` en motor |
| **Phase 67** | 🌳 **MAH Merger Trees**: `MassAccretionHistory`, `mah_main_branch`, `mah_mcbride2009`, CLI `gadget-ng mah` |
| **Phase 68** | 🔍 **SUBFIND**: `local_density_sph`, `find_subhalos`, `SubhaloRecord`, flag `--subfind` en `gadget-ng analyze` |
| **Phase 69** | 🚀 **Producción N=256³**: `configs/production_256.toml`, `scripts/run_production_256.sh`, notebooks Python P(k)/HMF, smoke tests N=32³ |
| **Phase 70** | 🔲 **AMR-PM**: `PatchGrid`, `amr_pm_accels`, zero-padding, identificación de parches por sobredensidad, 2 niveles jerárquicos |

---

## Trabajos futuros

Todas las Grandes originales están completadas. Posibles extensiones:

| Tarea | Descripción | Estimación |
|-------|-------------|-----------|
| G4 jerárquico | AMR de 3+ niveles recursivos + comunicación MPI de parches | 3–4 sesiones |
| RT | Transferencia radiativa (M1 closure, Morel) | 5–8 sesiones |
| Stellar feedback | Kick estocástico SN en gas SPH cercano | 2–3 sesiones |
| Bispectrum B(k) | Estadísticas de orden 3 del campo de densidad | 1–2 sesiones |

---

## Historial de fases (detalle técnico)

## Fase actual (MVP cerrado)

- Workspace Rust, CLI (`config`, `stepping`, `snapshot`), TOML + env.
- Gravedad directa suavizada, leapfrog KDK, MPI con `ParallelRuntime`.
- Tests: momento lineal (red cúbica), energía acotada (oscilador armónico en tests de integrador), paridad sub-bloque vs global, script serial/MPI.
- CI: `fmt`, `clippy -D warnings`, `test`, smoke MPI + paridad.

## Siguientes hitos

1. **Solvers de gravedad**: **Barnes–Hut monopolar** (octree, `gadget-ng-tree`) ✓; **PM periódico 3D** (`gadget-ng-pm`, CIC + FFT `rustfft` + Poisson, `SolverKind::Pm`) ✓; **TreePM** (`gadget-ng-treepm`, splitting Gaussiano en k-space, kernel erfc corto + PM erf largo, `SolverKind::TreePm`) ✓; pendiente FMM (multipolos de orden superior).
2. **I/O**: **HDF5** (GADGET-like) y **bincode** opcionales en `gadget-ng-io` ✓; **lectura unificada** (`SnapshotReader` + `reader_for` + `read_snapshot_formatted` para JSONL/bincode/HDF5) ✓; **MessagePack** (`SnapshotFormat::Msgpack`, `--features msgpack`, `rmp-serde`, `particles.msgpack`) ✓; **NetCDF-4** (`SnapshotFormat::Netcdf`, `--features netcdf`, layout SoA en `snapshot.nc` — variables x/y/z/vx/vy/vz/mass/id, atributos globales time/redshift/box_size, interoperable con `xarray`, `NCDatasets`) ✓.
3. **Rendimiento**: **Rayon** en bucle externo de partículas para `DirectGravity` y `BarnesHutGravity` (`RayonDirectGravity`, `RayonBarnesHutGravity`), activado con `[performance] deterministic = false` y `--features simd`; benchmarks Criterion en `gadget-ng-core` y `gadget-ng-tree` ✓; **SIMD a nivel de instrucción + caché-blocking** (`SimdDirectGravity` con SoA, `BLOCK_J=64`, `#[target_feature(avx2,fma)]`) ✓; **Rayon en PM / TreePM** ✓ — `assign_rayon` + `interpolate_rayon` (CIC thread-local fold), k-space paralelo en `fft_poisson`, `short_range_accels` paralelo en TreePM; feature `pm-rayon` en CLI.
4. **GPU**: crate `gadget-ng-gpu` (placeholder) ✓ — layout SoA (`GpuParticlesSoA`, 8 arrays planos), `GpuDirectGravity` stub con `GravitySolver` impl en `gadget_ng_core::gpu_bridge`; pendientes kernels reales (wgpu / CUDA / HIP) y `cfg.performance.use_gpu`.
5. **Pasos temporales**: **block timesteps al estilo GADGET-4** ✓ — `TimestepSection` en `config.rs` (`hierarchical`, `eta`, `max_level`); módulo `gadget-ng-integrators::hierarchical` con `HierarchicalState`, `aarseth_bin` y `hierarchical_kdk_step`; integración en `engine.rs` vía `cfg.timestep.hierarchical`; **predictor de Störmer para partículas inactivas** ✓ — `Δx_j = 0.5·a_j·(elapsed·fine_dt)²` reduce error de posición O(Δt²) → O(Δt³) en evaluación de fuerzas, sin alterar la integración simpléctica; **snapshot de HierarchicalState** ✓ — `HierarchicalState` deriva `Serialize/Deserialize`, `save(dir)/load(dir)` a `hierarchical_state.json`, guardado automático en `engine.rs` junto al snapshot final; pendiente: acoplamiento a árbol distribuido.
6. **Cosmología básica** ✓ — formulación de momentum canónico (estilo GADGET-4): `p = a²·dx_c/dt` en `particle.velocity`; `CosmologySection` en `config.rs` (`omega_m`, `omega_lambda`, `h0`, `a_init`); módulo `gadget-ng-core::cosmology` con `CosmologyParams`, `advance_a` (RK4 Friedmann) y `drift_kick_factors` (N_SUB=16, Simpson); **drifts con** `∫ dt/a²` y **kicks con** `∫ dt/a`; `leapfrog_cosmo_kdk_step` + `CosmoFactors` en `gadget-ng-integrators`; `hierarchical_kdk_step` extendido con `cosmo: Option<(&CosmologyParams, &mut f64)>` usando sumas prefijas de medios sub-pasos; `redshift = 1/a−1` en `SnapshotEnv`; retrocompatible (`enabled = false` → comportamiento Newtoniano sin cambios).
7. **FMM — Fast Multipole Method** ✓ — cuadrupolo + octupolo STF completos; tensor cuadrupolar `[Qxx,Qxy,Qxz,Qyy,Qyz,Qzz]` y tensor octupolar STF `[O_xxx,O_xxy,O_xxz,O_xyy,O_xyz,O_yyy,O_yzz]` en `OctNode`; `outer3_tf` (TF3 para punto de masa) + `oct_accel` (corrección de fuerza octupolar); teorema del eje paralelo para quad y oct en `aggregate`; M2L implícito: `walk_inner` aplica mono+quad+oct cuando MAC se satisface; L2L implícito: acumulación jerárquica del árbol; error relativo medio θ=0.4, N=1000 < 0.1 % vs directo; 5 tests unitarios (sin-traza, mejora de precisión, N=1000).
8. **GPU — kernels reales** ✓ — `GpuDirectGravity` real con wgpu 29 (WGSL compute shader); kernel O(N²) de Plummer suavizado en f32 (error relativo O(1e-7)); `GpuContext` con `Arc<>` + `Send+Sync`; readback síncrono via `PollType::wait_indefinitely()`; `[performance] use_gpu = true` en TOML activa la GPU con fallback automático a CPU si no hay adaptador; 3 tests unitarios GPU (se omiten en CI sin GPU); `use_gpu: bool` añadido a `PerformanceSection`. Pendiente: benchmarks GPU vs SIMD/Rayon, soporte CUDA/HIP.
9. **MPI — árbol distribuido** ✓ — `SlabDecomposition` (dominio x en slabs uniformes) en `gadget-ng-parallel::domain`; `allreduce_min/max_f64` en `ParallelRuntime`; `exchange_domain_by_x` (migración de partículas entre rangos) y `exchange_halos_by_x` (halos punto-a-punto con vecinos izquierdo/derecho, patrón odd-even anti-deadlock) implementados en `SerialRuntime` (trivial) y `MpiRuntime` (send/receive bloqueante); `compute_forces_local_tree` en engine construye árbol local de (partículas + halos) y evalúa fuerzas por índice local; activado con `[performance] use_distributed_tree = true` y `SolverKind::BarnesHut`; `halo_factor` configurable (default 0.5). Comunicación O(N_halo × 2) en lugar de Allgather O(N). 3 tests unitarios. Limitaciones conocidas: slab 1D en x únicamente (SFC/ORB pendiente); jerárquico y cosmológico sin soporte distribuido aún.

## Fase 2

10. **Pulir** ✓ — benchmarks Criterion en `gadget-ng-pm` (`pm_gravity`) y `gadget-ng-treepm` (`treepm_gravity`); `CHANGELOG.md` con historial semántico completo; `docs/user-guide.md` con ejemplos TOML para todos los solvers y opciones; `.github/workflows/ci.yml` — `fmt`, `clippy -D warnings`, `cargo test --workspace`, bench dry-run, cobertura con tarpaulin.
11. **FMM completo** ✓ — octupolo STF (7 componentes independientes) en `OctNode`; `outer3_tf(s, m)` para teorema del eje paralelo; `oct_accel(r, o, g)` con fórmula exacta `a^(oct) = G·[-O_{αβγ}r_βr_γ/(2|r|⁷) + (7/6)O_{βγδ}r_βr_γr_δr_α/|r|⁹]`; M2L implícito en `walk_inner` (mono+quad+oct al MAC); L2L implícito en la recursión del árbol; error < 0.1 % vs directo con N=1000, θ=0.4; 5 tests (sin-traza, mejora vs monopolo, N=1000).
12. **Restart / checkpointing completo** ✓ — `[output] checkpoint_interval = N` guarda checkpoint cada N pasos en `<out>/checkpoint/`; guarda partículas (JSONL), `HierarchicalState` y `checkpoint.json` (paso, factor de escala `a`, hash de config); `gadget-ng stepping --resume <out>` reanuda desde el último checkpoint en todos los modos (leapfrog, cosmo, jerárquico, árbol distribuido); advertencia si la config cambió.
13. **SFC + árbol distribuido mejorado** ✓ — `gadget-ng-parallel::sfc`: Morton Z-order 3D (`morton3`, `spread_bits`, 21 bits/eje, 63 bits totales); `SfcDecomposition::build` parte el dominio en N segmentos de igual tamaño por clave SFC (balanceo por número de partículas); `rank_for_pos`, `halo_width`; `partition_local` para migración many-to-many; `global_bbox` con allreduce; `exchange_domain_sfc` + `exchange_halos_sfc` en `ParallelRuntime` (serial: no-op; MPI: punto-a-punto odd-even igual que slab); `[performance] use_sfc = true` + `sfc_rebalance_interval = 10` activan el camino SFC en engine con rebalanceo dinámico; slab 1D en x conservado como fallback cuando `use_sfc = false`. 5 tests unitarios.
14. **Análisis in-situ** ✓ — crate `gadget-ng-analysis`; FoF con cell-linked-list + Union-Find (path-compression + union-by-rank): `find_halos(positions, velocities, masses, box_size, b, min_particles, rho_crit)` devuelve `Vec<FofHalo>` con `n_particles`, `mass`, COM, `v_com`, `velocity_dispersion`, `r_vir`; power spectrum P(k) via CIC + FFT 3D (rustfft, deconvolución sinc, N_bin = N/2): `power_spectrum(positions, masses, box_size, mesh)` → `Vec<PkBin {k, pk, n_modes}>`; catálogos JSONL: `write/read_halo_catalog`, `write/read_power_spectrum`; función `analyse(particles, params) -> AnalysisResult`; 9 tests (5 unitarios + 4 integración). Complejidad: alta.
15. **Sistema de unidades físicas** ✓ — `UnitsSection` en `config.rs` (`enabled`, `length_in_kpc`, `mass_in_msun`, `velocity_in_km_s`); `G_KPC_MSUN_KMPS = 4.3009×10⁻⁶`; `RunConfig::effective_g()` calcula G en unidades internas cuando `enabled = true`; `time_unit_in_gyr()` y `hubble_time(h0)`; `SnapshotEnv` y `meta.json` incluyen bloque `units` con `g_internal`, `time_in_gyr`; retrocompatible (`enabled = false` → `simulation.gravitational_constant` sin cambios).
16. **SPH básico** ✓ — crate `gadget-ng-sph`; `ParticleType {DarkMatter, Gas}` + `GasData {u, rho, pressure, h_sml, acc_sph, du_dt}` + `SphParticle`; kernel **Wendland C2** 3D normalizado (σ₃=21/(16π)); `compute_density` con suavizado adaptativo h_sml via Newton-Raphson (N_neigh=32); `compute_sph_forces` (ecuaciones simétricas Springel-Hernquist 2002 + viscosidad Monaghan α=1); EOS adiabática P=(γ-1)ρu, γ=5/3; integrador `sph_kdk_step` (leapfrog KDK grav+SPH+energía interna); 6 tests unitarios.
17. **Visualización en tiempo real** ✓ — crate `gadget-ng-vis`; `CpuCanvas` RGBA con backend CPU (sin dependencia GPU en runtime); `Projection {XY, XZ, YZ, Perspective}` configurable; `ColorMode {White, Velocity, Density}` con mapa Viridis; `Renderer::render_frame` + `render_frame_with_scalars`; `save_png` via crate `png`; `frame_path(dir, step)` genera `snap_{step:06}.png`; 9 tests (proyección, canvas, export PNG, renderer).
18. **Brechas GADGET-4 (Phases 43-47)** ✓
19. **Halofit no-lineal (Phase 48)** ✓
20. **Fix integrador cosmológico (Phase 49)** ✓
21. **Unidades físicamente consistentes (Phase 50)** ✓
22. **G auto-consistente en motor de producción (Phase 51)** ✓
23. **Función de masa de halos Press-Schechter / Sheth-Tormen (Phase 52)** ✓
24. **Perfiles NFW y relación concentración-masa (Phase 53)** ✓
25. **Validación cuantitativa D²(a) con G consistente (Phase 54)** ✓ — `phase54_growth_factor_validation.rs`: evolución PM con timestep adaptativo (`adaptive_dt_cosmo`, α_H=0.01, dt_max=0.05) y `G_consistent = 3Ω_m H₀²/(8π) ≈ 3.76e-4`; N ∈ {64,128,256}, BOX=100 Mpc/h, seed=42, 6 snapshots a ∈ {0.02,0.05,0.10,0.20,0.33,0.50}; métricas: P(k,a)/P(k,a₀) vs [D(a)/D(a₀)]² en bins k < k_nyq/2; tolerancias: 30% (N=64), 15% (N=128), 10% (N=256); convergencia monotónica verificada; 5 tests + script `run_phase54.sh`; JSON con resultados en `target/phase54/snapshots.json`.
26. **Comparación espectro de masas FoF vs HMF (Phase 55)** ✓ — `phase55_fof_vs_hmf.rs`: evolución hasta a=1.0 (z=0) con G consistente y timestep adaptativo; N ∈ {64,128,256}, BOX=300 Mpc/h; FoF (b=0.2, min_particles=20) en unidades internas; conversión física: m_part = Ω_m·ρ_crit_H2·BOX³_Mpc_h/N_total; comparación dn/dlnM(FoF) vs dn/dlnM(ST/PS) con tolerancia ratio ∈ [0.05,20]; masa mínima resoluble: 1.8×10¹⁴ (N=64), 2.2×10¹³ (N=128), 2.8×10¹² (N=256) M_sun/h; convergencia de masa mínima verificada (N=256 < N=64); 6 tests + script `run_phase55.sh`; JSON con catálogos en `target/phase55/fof_results.json`. — `gadget_ng_analysis::nfw`: `NfwProfile {rho_s, r_s}` con `from_m200_c(M_200, c, ρ_crit)`, `density(r)`, `mass_enclosed(r)`, `r200(ρ_crit)`, `circular_velocity_sq_over_g(r)`, `concentration(ρ_crit)`; `r200_from_m200`, `rho_crit_z(Ω_m, Ω_Λ, z)`, `concentration_duffy2008(M, z)` (WMAP5: A=5.71, B=-0.084, C=-0.47), `concentration_bhattacharya2013`; `measure_density_profile(radii, m_part, ...)→Vec<DensityBin>` con bins log-espaciados; `fit_nfw_concentration(bins, M_200, ρ_crit, c_min, c_max, N)→Option<NfwFitResult>` búsqueda en cuadrícula + LS en log-espacio; propiedades analíticas verificadas: M(<r_200)=M_200 y ρ_mean=200ρ_crit con error < 10⁻¹⁰, pendientes γ=-1/-2/-3 (err<0.05), v_c_max en r/r_s=2.163±0.3, c_fit=5.27 vs c_true=5.0 (err<6%); tabla z=0: c(10¹²)=6.05, c(10¹⁴)=4.11, c(10¹⁵)=3.39; 14 tests (6 integración + 8 unitarios). — `gadget_ng_analysis::halo_mass_function`: `sigma_m(M, params, z)` calcula σ(M,z) = D(z)·σ(M,0) con integral `σ²(R)=(1/2π²)∫k³P(k)W²(kR)dk` trapezoidal log-espaciada (1200 puntos); `lagrange_radius(M, ρ̄_m)` convierte masa a radio de Lagrange; `mass_function_table(params, M_min, M_max, N, z)→Vec<HmfBin>` genera tabla dn/d ln M para PS y ST; `total_halo_density(table)→(n_PS, n_ST)` integra la tabla; funciones auxiliares `multiplicity_ps(σ)` y `multiplicity_st(σ)` devuelven f(σ) para las dos multiplicidades; `HmfParams::planck2018()` preset Planck 2018; `RHO_CRIT_H2 = 2.775×10¹¹ (M_sun/h)/(Mpc/h)³`; normalización verificada: σ(R=8 Mpc/h)=σ₈ con error < 0.01 %; cómputo de la tabla con P(k)=amp²×k^n_s×T²_EH (convención IC correcta); 7 tests en `phase52_mass_function.rs`: (1) σ(R=8)=σ₈ < 0.01% error, (2) perfil σ(M) monótono con rangos físicos, (3) ∫f_PS dσ=0.998≈1, ∫f_ST dσ=0.953≈1, (4) tabla z=0 coherente y monótona, (5) σ(M,z=1)<σ(M,z=0)—formación jerárquica, (6) n(>10¹⁴)≈3.2×10⁻⁵ h³/Mpc³ coherente con ACT/SPT/eROSITA, (7) ICs+FoF cualitativo. — `CosmologySection::auto_g: bool` en `config.rs`; cuando `auto_g = true` y `cosmology.enabled = true`, `effective_g()` calcula `G = 3·Ω_m·H₀²/(8π)` (prioridad 2 tras `units.enabled`); `RunConfig::cosmo_g_diagnostic() → Option<(f64, f64)>` devuelve `(G_consistente, error_relativo)` para cualquier config cosmológica; motor `engine.rs` emite `warn!` si G manual difiere > 1% del valor Friedmann-consistente, e `info!` cuando `auto_g=true` activo; 5 tests en `phase51_auto_g.rs`: `effective_g` con `auto_g=true` (err=0), diagnóstico G_legacy (265854%), G manual consistente (err=0), prioridad `UnitsSection` sobre `auto_g` (G_units=4.3e4 ≫ G_auto=3.76e-4), simulación estable con `auto_g=true` (a=0.02→0.04, v_rms=1.68e-6); retrocompatible: `auto_g = false` (default) preserva comportamiento anterior exacto. — `g_code_consistent(omega_m, h0) → f64` en `cosmology.rs`: calcula G en unidades de código para que la ecuación de Friedmann se satisfaga con ρ̄_m=1 en caja unitaria: `G = 3·Ω_m·H₀²/(8π)`; `cosmo_consistency_error(g, omega_m, h0, rho_bar)` para verificar y diagnosticar inconsistencias; ambas exportadas desde `gadget-ng-core::lib.rs`; diagnóstico cuantitativo: con G_legacy=1 y H₀=0.1, `(4πGρ̄)/H₀² = 1257` (factor 2660× fuera de `(3/2)Ω_m = 0.47`); 5 tests en `phase50_physical_units.rs`: (1) fórmula algebraica exacta, (2) cuantificación de la inconsistencia legacy, (3) estabilidad/crecimiento corto N=8 con G_consistente, (4) estabilidad larga a=0.02→0.20 (516 pasos, P_ratio=5.61), (5) comparación G_consistente vs G_legacy; la verificación cuantitativa de D²(a) requiere N≥64 en release (baja resolución en debug imposibilita CV suficiente). — `adaptive_dt_cosmo(params, a, acc_max, softening, eta_grav, alpha_h, dt_max)` en `cosmology.rs`; criterios gravitacional `dt_grav = η·√(ε/|a_max|)` + Hubble `dt_hub = α_H/H(a)`; diagnóstico A/B coupling (G/a vs G·a³) confirma G·a³ correcto; convergencia timestep (fijo vs adaptativo < 0.3 %); tests de estabilidad a=0.02→0.5; corrección de coupling histórico incorrecto (G/a → `gravity_coupling_qksl`) en `cosmo_pm.rs`, `phase37_growth_rescaled_ics.rs` y `phase41_high_resolution_validation.rs`; validación Halofit con integrador corregido (3 tests); 10 tests en 4 archivos nuevos. Nota: inconsistencia G=1/H₀=0.1 en parámetros de test impide verificar D²(a) para evoluciones largas — cuantificada y documentada. — `gadget_ng_analysis::halofit`: `halofit_pk(k, p_lin, cosmo, z)` implementa Takahashi+2012 (ec. 11–35); `sigma_sq(R)` con integración log-trapezoidal; bisección para `k_sigma`; `n_eff` y curvatura `C` via diferencias finitas; coeficientes de ajuste {an,bn,cn,γ,α,β,ν} para ΛCDM plano; `p_linear_eh` conveniente con factor D²(a); 7 tests unitarios (σ(8)=σ₈, k_sigma razonable, P_nl≥P_lin, convergencia lineal, boost no-lineal, ratios vs CAMB, k_sigma crece con z); 4 tests de integración (`halofit_static`, `halofit_growth_consistency` < 3.5 % error, `pk_vs_halofit_at_ics` < 50 %, `nonlinear_boost_redshift_dependence`); limitación conocida: EH da boost ~6 % en k=0.3 vs ~15 % de CAMB — aceptable para uso interno. — (a) *Block timesteps cosmológicos* (Phase 43/44): `hierarchical_kdk_step` extendido con `kappa_h: Option<f64>` para acoplamiento correcto `gravity_coupling_qksl(g, a)`; diagnósticos `CosmoDiag` en engine; (b) *Balanceo por coste de árbol* (Phase 43): `SfcDecomposition::build_weighted` con EMA sobre `WalkStats.opened_nodes`; (c) *PM Pencil 2D* (Phase 46): `PencilLayout2D`, `solve_forces_pencil2d`, `alltoallv_f64_subgroup` en `ParallelRuntime`; escala hasta `P ≤ nm²` en lugar de `P ≤ nm`; selección automática cuando `P > nm`; (d) *Unidades QKSL* (Phase 45): fix `g_cosmo = G·a³` en lugar de `G/a`; (e) *Corrección P(k) recalibrada* (Phase 47): `measure_rn()` para calibrar R(N) in-process; `correct_pk_with_shot_noise()` para sustracción de ruido Poisson; `RnModel::phase47_default()` con R(N=128)=0.002252 (campaña 4 seeds, CV=1.0%); fit {32,64,128}: `α=1.953`.
